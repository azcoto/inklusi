{
  "name": "server",
  "private": true,
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --poll --respawn --transpile-only src/index.ts "
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "config": "*",
    "nodemon": "^2.0.16",
    "ts-node-dev": "^1.1.8",
    "tsconfig": "*"
  }
}
